var helloWorld = "Hello World";
function getAdminUser(user) {
    //...
}
function deleteUser(user) {
    // ...
}
class UserAccount {
    constructor(name, id) {
        this.name = name;
        this.id = id;
    }
}
const user = new UserAccount("Hailfire", 1);
function getLength(obj) {
    return obj.length;
}
function wrapInArray(obj) {
    if (typeof obj === "string") {
        return [obj];
    }
    else {
        return obj;
    }
}
const object = champion.get();
champion.add("Sivir");
message.toLowerCase;
const message = "Hello World";
message();
function fn(x) {
    return x.flip();
}
const user = {
    name: "Hailfire",
    age: 25,
};
user.location;

